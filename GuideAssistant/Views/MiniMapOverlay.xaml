<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="GuideAssistant.Views.MiniMapOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="小地图">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop/>
    </Window.SystemBackdrop>
    
    <Grid>
        <Border x:Name="MapBorder" Width="180" Height="180" CornerRadius="90"
                Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Center"
                BorderBrush="#44888888" BorderThickness="1">
            <Grid>
                <!-- Compass cross -->
                <Line X1="90" Y1="10" X2="90" Y2="170" Stroke="#44888888" StrokeThickness="1" />
                <Line X1="10" Y1="90" X2="170" Y2="90" Stroke="#44888888" StrokeThickness="1" />

                <!-- Cardinal labels -->
                <TextBlock Text="N" FontSize="11" Foreground="#88FFFFFF"
                           HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,8,0,0" />
                <TextBlock Text="S" FontSize="11" Foreground="#88FFFFFF"
                           HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,8" />
                <TextBlock Text="W" FontSize="11" Foreground="#88FFFFFF"
                           HorizontalAlignment="Left" VerticalAlignment="Center" Margin="8,0,0,0" />
                <TextBlock Text="E" FontSize="11" Foreground="#88FFFFFF"
                           HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0" />

                <!-- Arrow container -->
                <Canvas x:Name="ArrowCanvas" />
            </Grid>
        </Border>
    </Grid>
</Window>
