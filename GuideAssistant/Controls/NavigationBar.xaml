<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GuideAssistant.Controls.NavigationBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Height="40">

    <Grid Padding="8,4" Background="{ThemeResource SystemChromeMediumLowColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Back -->
        <Button x:Name="BackBtn" Grid.Column="0" Content="←" FontSize="14" Width="34" Height="30"
                Background="Transparent" BorderThickness="0" Margin="0,0,2,0"
                Click="BackBtn_Click" />

        <!-- Forward -->
        <Button x:Name="ForwardBtn" Grid.Column="1" Content="→" FontSize="14" Width="34" Height="30"
                Background="Transparent" BorderThickness="0" Margin="0,0,2,0"
                Click="ForwardBtn_Click" />

        <!-- Refresh -->
        <Button x:Name="RefreshBtn" Grid.Column="2" Content="↻" FontSize="14" Width="34" Height="30"
                Background="Transparent" BorderThickness="0" Margin="0,0,4,0"
                Click="RefreshBtn_Click" />

        <!-- URL bar -->
        <AutoSuggestBox x:Name="UrlBox" Grid.Column="3" Height="30" VerticalAlignment="Center"
                         PlaceholderText="输入网址或搜索..." FontSize="13"
                         QueryIcon="Find" QuerySubmitted="UrlBox_QuerySubmitted"
                         KeyDown="UrlBox_KeyDown" />

        <!-- Bookmark -->
        <Button x:Name="BookmarkBtn" Grid.Column="4" Content="☆" FontSize="16" Width="34" Height="30"
                Background="Transparent" BorderThickness="0" Margin="2,0,0,0"
                Click="BookmarkBtn_Click" />

        <!-- Settings -->
        <Button x:Name="SettingsBtn" Grid.Column="5" Content="⚙" FontSize="16" Width="34" Height="30"
                Background="Transparent" BorderThickness="0" Margin="2,0,0,0"
                Click="SettingsBtn_Click" />
    </Grid>
</UserControl>
